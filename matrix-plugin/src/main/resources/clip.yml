name: ${pluginName}
version: ${version}
main: ${group}.${packageName}.plugin.${pluginName}TestPlugin
api-version: ${paperVersion}
libraries:
  - org.jetbrains.kotlin:kotlin-stdlib:${kotlinVersion}
  - org.jetbrains.kotlin:kotlin-reflect:${kotlinVersion}
  - ${group}:${packageName}-core:${version}.${core_fixed}